# Ignore local development files and folders
.venv/
__pycache__/
*.pyc

# Vercel's serverless functions have a read-only filesystem.
# These folders are written to at runtime in your local version,
# so we must exclude them from the deployment bundle.
# We will use Vercel's temporary directory for uploads instead.
data/
static/uploads/
faiss_index/
